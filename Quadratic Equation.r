# assigns values to variables
a <- 1
b <- 1
c <- -1

# test case 1 to solve the quadratic equation
(-b + sqrt(b^2 - 4*a*c) / (2*a))
(-b - sqrt(b^2 - 4*a*c) / (2*a))

# test case 2 using the correct formula
(-b + sqrt(b^2 - 4*a*c)) / (2*a)
(-b - sqrt(b^2 - 4*a*c)) / (2*a)

# assisgnin the solutions
solution1 <- (-b + sqrt(b^2 - 4*a*c)) / (2*a)
solution2 <- (-b - sqrt(b^2 - 4*a*c)) / (2*a)
